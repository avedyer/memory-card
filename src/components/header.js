import React from 'react';
import title from '../imgs/title.png'

const Header = () => {
    return (
        <header>
            <img src={title} alt='MEMORY TIME'/>
        </header>
    )
}

export default Header